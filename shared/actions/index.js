import * as url from './url';
import { changeLanguage, setMessages, setEmptyLocalization } from './intl';

export {
  // eslint-disable-next-line import/prefer-default-export
  url,
  changeLanguage,
  setMessages,
  setEmptyLocalization,
};

/* eslint-disable no-undef, import/no-extraneous-dependencies, import/no-unresolved, import/extensions, max-len */
import Enum from 'es6-enum';
/* eslint-enable */

const PLATFORM = Enum('SET');

export default PLATFORM;
